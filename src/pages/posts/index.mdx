---
title: 시작하기
date: 2024-10-04 00:12:20 +0900
tags: ['navigation']
---

import { Cards } from "nextra/components";
import NextraIcon from "/public/assets/nextra-icon/icons";

# 시작하기

## 웹 개발

<Cards>
	{[
		{ title: "Node.js", href: "nodejs", icon: "nodejs", size: 22 },
		{ title: "CSS", href: "css", icon: "css", size: 26 },
		{ title: "Javascript", href: "javascript", icon: "javascript", size: 20 },
	].map((card, index) => (
		<Cards.Card
			key={index}
			icon={<NextraIcon name={card.icon} width={card.size} height={card.size} />}
			title={card.title}
			href={"/posts/" + card.href}
			children=""
			arrow
		/>
	))}
</Cards>

## 서버

<Cards>
	{[
		{ title: "Docker", href: "docker", icon: "docker", extension: "webp", size: 26 },
		{ title: "Jenkins", href: "jenkins", icon: "jenkins", size: 28 },
	].map((card, index) => (
		<Cards.Card
			key={index}
			icon={
				<NextraIcon
					name={card.icon}
					extension={card.extension ?? undefined}
					width={card.size}
					height={card.size}
				/>
			}
			title={card.title}
			href={"/posts/" + card.href}
			children=""
			arrow
		/>
	))}
</Cards>

## 알고리즘

<Cards>
	<Cards.Card
		icon={<NextraIcon name="programmers" width={20} height={20} />}
		title="프로그래머스"
		href="/posts/programmers"
		children=""
		arrow
	/>
</Cards>

## 토이 프로젝트

<Cards>
	{[
		{ title: "MariOver", href: "mariover", icon: "tgr", size: 24 },
	].map((card, index) => (
		<Cards.Card
			key={index}
			icon={<NextraIcon name={card.icon} width={card.size} height={card.size} />}
			title={card.title}
			href={"/posts/" + card.href}
			children=""
			arrow
		/>
	))}
</Cards>

## 기타

<Cards>
	{[
		{ title: "기술 면접", href: "interview", icon: "interview", size: 22 },
		{ title: "예외 처리", href: "exception", icon: "retry", size: 22 },
		{ title: "윤하 2024", href: "yh-2024", icon: "music", size: 22 },
	].map((card, index) => (
		<Cards.Card
			key={index}
			icon={<NextraIcon name={card.icon} width={card.size} height={card.size} />}
			title={card.title}
			href={"/posts/" + card.href}
			children=""
			arrow
		/>
	))}
</Cards>