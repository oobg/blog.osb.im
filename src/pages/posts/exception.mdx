---
title: 예외 처리 목차
date: 2024-10-31 01:40:00 +0900
tags: ['navigation']
---

import { Cards, Card } from "nextra/components";
import NextraIcon from "/public/assets/nextra-icon/icons";

# 예외 처리

<Cards>
	{[
		{ title: "Input 태그 숫자 입력 제한" },
	].map((card, index) => (
		<Card
			key={index}
			icon={<NextraIcon name="exception" />}
			title={card.title}
			href={"/posts/exception/" + index.toString().padStart(2, "0")}
			children=""
			arrow
		/>
	))}
</Cards>